# Frontend runtime configuration (Vite reads variables prefixed with VITE_)

# Backend API base URL. In local dev leave it as /api so requests go through
# the Vite proxy (see vite.config.ts), which forwards them to the backend on
# http://localhost:3001. Override in CI/prod with the full URL of the API.
VITE_API_BASE_URL=/api
